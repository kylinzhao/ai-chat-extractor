# 图片显示去重修复

**提交哈希:** `dd69ede`
**完成时间:** 2026-02-10
**状态:** ✅ 已完成并推送

---

## 问题描述

用户反馈：页面上显示 8 张图片，但只需要每种风格生成一张。

### 根本原因

1. **重新生成图片会添加新图片**
   - 每次点击"重新生成"都会创建新的渲染任务
   - 新图片 URL 会被追加到 `conversation.image_urls` 数组
   - 数据库会保存所有历史图片

2. **前端显示所有图片**
   - `ImageGallery` 组件直接显示 `image_urls` 数组中的所有图片
   - 没有去重逻辑，导致重复图片显示
   - 如果重新生成 8 次，就会显示 8 张图片

---

## 解决方案

### 修改文件：`web/app/components/ImageGallery.tsx`

#### 添加去重逻辑

```typescript
// Parse image URLs to extract template type
// Format: /public/renders/{conversationId}-{templateType}-{timestamp}.png
const parsedImages = images.map(url => {
  const filename = url.split('/').pop() || '';
  const parts = filename.replace('.png', '').split('-');
  const template = parts[parts.length - 2] || 'unknown';
  return {
    url,
    template,
    timestamp: parts[parts.length - 1],
  };
});

// Sort by timestamp descending (newest first)
const sortedImages = [...parsedImages].sort((a, b) => {
  return (b.timestamp || '').localeCompare(a.timestamp || '');
});

// Keep only the newest image for each template type
const uniqueImages = sortedImages.filter((image, index, self) => {
  // Find the first occurrence of this template type (which is the newest due to sorting)
  const firstOccurrence = self.findIndex(img => img.template === image.template);
  return index === firstOccurrence;
});
```

#### 更新显示逻辑

**修改前：**
```typescript
<h2>🖼️ 渲染图片 ({images.length})</h2>
...
{sortedImages.map((image, index) => {
```

**修改后：**
```typescript
<h2>🖼️ 渲染图片 ({uniqueImages.length})</h2>
...
{uniqueImages.map((image, index) => {
```

---

## 实现原理

### URL 格式解析

图片 URL 格式：
```
/public/renders/{conversationId}-{templateType}-{timestamp}.png
```

示例：
```
/public/renders/1-bento-1739212345678.png
/public/renders/1-newsletter-1739212348123.png
/public/renders/1-retro_letter-1739212350456.png
```

### 去重逻辑

1. **解析 URL** → 提取 `templateType` 和 `timestamp`
2. **排序** → 按 `timestamp` 降序（最新的在前）
3. **去重** → 对每种 `templateType` 只保留第一个（最新的）

### 效果

| 模板类型 | 处理前 | 处理后 |
|---------|--------|--------|
| bento | 3 张（显示所有） | 1 张（最新的） |
| newsletter | 3 张（显示所有） | 1 张（最新的） |
| retro_letter | 2 张（显示所有） | 1 张（最新的） |
| **总计** | **8 张** ❌ | **3 张** ✅ |

---

## 测试验证

### 测试场景

1. **多种模板图片**
   - 假设数据库中有 8 张图片（3 张 bento, 3 张 newsletter, 2 张 retro_letter）
   - 期望：只显示 3 张（每种风格最新的一张）

2. **单种模板图片**
   - 假设数据库中有 5 张图片（全部是 bento）
   - 期望：只显示 1 张（最新的 bento）

3. **空图片**
   - 期望：显示"暂无渲染图片"

### 验证方法

访问任意对话详情页：
```
http://localhost:3001/public/1
```

检查：
- ✅ 图片总数不超过 3 张（或模板数量）
- ✅ 每种模板只显示一张
- ✅ 显示的是最新的图片（按时间戳）

---

## 代码提交

```bash
git add web/app/components/ImageGallery.tsx
git commit -m "fix: 只显示每种模板风格最新的图片"
git push origin main
```

**提交哈希:** `dd69ede`

---

## 后续优化建议

### 1. 清理旧图片
当前数据库保留了所有历史图片，可以添加清理功能：
```sql
-- 删除除最新图片外的所有旧图片
DELETE FROM conversations WHERE id NOT IN (
  SELECT MAX(id) FROM conversations GROUP BY conversation_id, template_type
);
```

### 2. 自动清理逻辑
在渲染新图片时自动删除旧图片：
```typescript
// 在 render-queue.ts 中
// 1. 查找该模板的旧图片
// 2. 删除旧图片文件
// 3. 从数据库移除旧 URL
// 4. 添加新图片 URL
```

### 3. 图片管理界面
添加后台管理界面：
- 查看所有图片
- 手动删除不需要的图片
- 批量清理旧图片

---

## 已知限制

1. **数据库仍保留所有图片**
   - 前端只是过滤显示
   - 数据库 `image_urls` 数组仍在增长
   - 长期可能占用较多存储空间

2. **文件系统未清理**
   - 旧的图片文件仍在 `public/renders/` 目录
   - 需要定期清理

3. **无用户选择**
   - 用户无法选择保留哪个版本
   - 始终显示最新的（按时间戳）

---

**修复完成！** 🎉

*Generated by Claude Sonnet 4.5*
*Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>*
